# Path to saved model artifact (joblib file)
MODEL_PATH=models/churn_model.joblib

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# API authentication key (set a strong secret in production)
API_KEY=changeme-super-secret-key
